// Exemplos do usdo do Math - Os exemplos são do site do Mozila

//Pesquisar mais sobre

function getRandom() {
  return Math.random()
}

function getRandomArbitraty(min, max) {
  return Math.random() * (max - min) + min
}
